# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20          # file uploads for doc ingestion

# --- LangChain (provider-agnostic core) ---
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14

# --- Swappable model providers (install/enable per your keys) ---
langchain-anthropic==0.3.1        # Claude
langchain-groq==0.2.3             # Groq (free tier for testing)
langchain-openai==0.2.14          # OpenAI-compatible + OpenAI embeddings option

# --- Vector store (pgvector on the shared Postgres) ---
langchain-postgres==0.0.13
psycopg[binary]==3.2.3
pgvector==0.3.6

# --- Embeddings: hosted via OpenAI (text-embedding-3-small, multilingual). ---
# No local ML stack — keeps the image ~200MB and RAM low. The OpenAI client
# comes in via langchain-openai above. Set EMBEDDINGS_PROVIDER=openai + an
# OPENAI_API_KEY. (To run fully offline instead, install requirements-local.txt
# which adds sentence-transformers + torch, and set EMBEDDINGS_PROVIDER=local.)

# --- Document loading for the knowledge base ---
pypdf==5.1.0
python-docx==1.1.2

# --- Escalation email + misc ---
httpx==0.28.1
slowapi==0.1.9                    # rate limiting
tenacity==9.0.0
