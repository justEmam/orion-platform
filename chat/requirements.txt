# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- LangChain (provider-agnostic core) ---
langchain==0.3.14
langchain-core==0.3.29

# --- Swappable chat providers (pick one via CHAT_PROVIDER) ---
langchain-openai==0.2.14          # OpenAI (default: gpt-4o-mini)
langchain-anthropic==0.3.1        # Claude (1-line swap)
langchain-groq==0.2.3             # Groq (1-line swap)

# --- Postgres (plain knowledge table + escalation store; NO pgvector/RAG) ---
psycopg[binary]==3.2.3

# --- HTTP (escalations -> CMS) ---
httpx==0.28.1
# (rate limiting is a small custom per-IP limiter, see app/ratelimit.py — no dep)
